/** @type {import('tailwindcss').Config} */
module.exports = {
  // 1. Set darkMode to 'media' (default, but good to be explicit)
  darkMode: "media",
  content: [
    // ... your content files
  ],
  // ... rest of config
};
